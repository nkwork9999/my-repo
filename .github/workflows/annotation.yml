name: Annotation
on: push
jobs:
  log:
    runs-on: ubuntu-latest
    steps:
      - run: echo "::error::This is an error"
      - run: echo "::waiting::This is an warning"
      - run: echo "::notice::This is a notice"
